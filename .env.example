# Copia este archivo a .env.local y completa con los valores reales
# del proyecto de Supabase (Project Settings -> API en supabase.com).
# .env.local nunca se sube a git (ver .gitignore).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
